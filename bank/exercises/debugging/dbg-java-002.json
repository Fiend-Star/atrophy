{
  "id": "dbg-java-002",
  "kind": "fix",
  "axis": "debugging",
  "language": "java",
  "tier": 1,
  "title": "Lookup finds small readings but not large ones",
  "prompt": "firstIndexOf(List<Integer> values, int target) must return the index of the first\nelement equal to target, or -1 when target is absent.\n\nThe on-call report: it finds 9 in [5, 9, 13] just fine, but says 256 is missing from\n[128, 256, 512]. Nothing about the list changed - only how big the numbers are.\nFind the bug and fix it; the loop itself is fine.\n\nKeep the class name Solution and the given method signature.",
  "functionName": "firstIndexOf",
  "starterCode": "import java.util.List;\n\npublic class Solution {\n    static int firstIndexOf(List<Integer> values, int target) {\n        Integer wanted = target;\n        for (int i = 0; i < values.size(); i++) {\n            if (values.get(i) == wanted) {\n                return i;\n            }\n        }\n        return -1;\n    }\n}\n",
  "softTimeLimitSeconds": 300,
  "testTimeoutMs": 20000,
  "tests": [
    { "args": [[5, 9, 13], 9], "expected": 1 },
    { "args": [[128, 256, 512], 256], "expected": 1 },
    { "args": [[1000, 2000, 3000], 3000], "expected": 2 },
    { "args": [[127, 128], 128], "expected": 1 },
    { "args": [[-200, 200], -200], "expected": 0 },
    { "args": [[], 7], "expected": -1 },
    { "args": [[4, 4, 4], 4], "expected": 0 },
    { "args": [[10, 20, 30], 99], "expected": -1 }
  ]
}
